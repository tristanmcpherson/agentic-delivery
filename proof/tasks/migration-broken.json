{
  "schema_version": 2,
  "contract_version": 1,
  "task_id": "PROOF-MIGRATION-BROKEN",
  "title": "Reject a green migration command that loses the backfill",
  "planning": { "size": "S", "size_source": "inferred", "confidence": "high" },
  "risk_flags": ["persistence", "migration"],
  "acceptance": [
    { "id": "AC-DATA", "surface": "data", "behavior": "Migration 002 preserves the existing profile value in display_name." }
  ],
  "checks": [
    {
      "id": "migration-broken",
      "criterion_ids": ["AC-DATA"],
      "claim_scope": "The migration preserves seeded data as well as applying the schema change.",
      "stage": "integration",
      "command": "node --no-warnings proof/fixture/non-ui/migration-check.mjs --broken",
      "required": true,
      "artifacts": {
        "system_attestation": {
          "kind": "migration",
          "required_assertions": ["migration-applied", "schema-current", "data-preserved", "rollback-compatible"]
        }
      }
    }
  ],
  "approval_boundaries": [],
  "evidence_expires_on": ["candidate-change", "contract-change", "harness-change", "runtime-change"]
}
