{
  "schema_version": 2,
  "contract_version": 1,
  "task_id": "PROOF-BROKEN",
  "title": "Catch a real API contract mismatch",
  "planning": { "size": "S", "size_source": "inferred", "confidence": "high" },
  "risk_flags": ["api-contract", "ui-behavior", "visual"],
  "acceptance": [
    { "id": "AC-1", "surface": "ui", "behavior": "The UI renders the nonce-derived profile returned by the real API." }
  ],
  "checks": [
    {
      "id": "ui-real-broken",
      "criterion_ids": ["AC-1"],
      "claim_scope": "The candidate UI consumes the declared local first-party API contract.",
      "stage": "ui",
      "profile": "local-broken",
      "command": "npx playwright test proof/fixture/tests/real.spec.mjs --config=proof/fixture/playwright.config.mjs",
      "expected_tests": ["real service profile journey"],
      "business_request": { "method": "GET", "path": "/api/profile" },
      "required": true,
      "safe_for_live": false,
      "artifacts": {
        "attestation": true,
        "test_integrity": true,
        "business_flow_provenance": true,
        "screenshots_min": 1,
        "visual_review": true
      }
    }
  ],
  "approval_boundaries": [],
  "evidence_expires_on": ["candidate-change", "contract-change", "profile-change", "harness-change"]
}
